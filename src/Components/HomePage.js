import Base from "../Base/Base"

export default function HomePage(){
    return(
        <Base
    title={"Welcome to students data page"}
    description={"Here you can add, delete, edit and update students data"}
    >
    </Base>
    );
}